# Decoded Pump.fun corpus subset (synthetic schema, not chain txs)

JSONL `CorpusRecord` rows for importer tests. `source_kind = DECODED_RESEARCH_CORPUS`.

Not the Hugging Face parquet. Dead mint has no trades so the graduation-bias detector sees mixed population.
